name: Claude AI Review

on:
  pull_request:
    branches:
      - main

jobs:
  ai-self-test:
    name: AI Self-Test
    runs-on: ubuntu-latest
    steps:
      - name: AI 自测通过
        run: echo "✅ AI 自测已通过"
